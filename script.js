// step one: declare variable for the string
const alertString =
  "You have received this message because you have been chosen to open an important vault. Here is the secret combination:";
// log the variable to verify it works
// console.log(alertString);

// declare variables using mathematical operands for the code
const vaultOne = 2 + 8;
const vaultTwo = 4 * 10;
const vaultThree = 40 - 1;
// log the variables to verify they work
// console.log(vaultOne);
// console.log(vaultTwo);
// console.log(vaultThree);

// create an alert with provided string and variables
alert(`${alertString} ${vaultOne} - ${vaultTwo} - ${vaultThree}`);
